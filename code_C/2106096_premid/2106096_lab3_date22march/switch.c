/*switch case only takes char(converted into ascii, so this is also int)(not char variable tho) and int values for logical checking of condition.
any case should have 1 statement or condition.

int x,y,z;
char a;
float f;

valid cases --> switch(x),switch('a'),case 2,case 'b'      a is character and not a variable.
invalid case --> switch(f),switch(a),case 2.5,case x*/